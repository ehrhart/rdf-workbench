# RDF Workbench frontend

This is the Next.js application. Provider composition lives under `providers/`,
while shared contracts and runtime selection live under `lib/runtime/`.

Use the root [README](../README.md) for configuration, startup, feature matrix,
and verification instructions.

```bash
pnpm install
pnpm dev
pnpm lint
pnpm build
```
